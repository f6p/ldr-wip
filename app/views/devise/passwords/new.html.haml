- title 'Reset Password'

= devise_form(password_path resource_name) do |f|
  = devise_error_messages!

  %fieldset.single
    = f.label       :email
    = f.email_field :email

  %fieldset
    = f.submit 'Reset Password'

= render 'devise/shared/links'
