#!/bin/bash

BACKUP_FILE=$OPENSHIFT_DATA_DIR$OPENSHIFT_APP_NAME-$(date +%Y%m%d).sql.gz
pg_dump --host $OPENSHIFT_POSTGRESQL_DB_HOST --port $OPENSHIFT_POSTGRESQL_DB_PORT --username $OPENSHIFT_POSTGRESQL_DB_USERNAME $OPENSHIFT_APP_NAME | gzip > $BACKUP_FILE
